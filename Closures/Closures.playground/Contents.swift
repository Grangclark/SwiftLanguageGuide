import UIKit

// クロージャ(Closures)
// 名前付き関数を作成せずに一緒に実行するコードをグループ化する。
// クロージャは、コード内で受け渡して使用できる、ある機能の独立したブロックです。
// Swift のクロージャは、他のプログラミング言語におけるクロージャ、匿名関数、ラムダ、ブロックと似ています。
// クロージャは、定数と変数への参照を、それらを定義したコンテキストからキャプチャして保持できます。
// これは、これらの定数と変数をスコープに閉じ込めると呼ばれます。
// Swift は、キャプチャに関連した全てのメモリ管理を行います。

// ただ、さっぱりわからん
